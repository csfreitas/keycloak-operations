package io.github.keycloakmcp.assessment.engine;

/**
 * Confidence derived from evidence source coverage (deterministic, not LLM).
 */
public enum AssessmentConfidence {
    HIGH,
    MEDIUM,
    LOW
}
