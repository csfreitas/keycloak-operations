apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak-mcp-assessor
  namespace: keycloak-mcp
  labels:
    app.kubernetes.io/name: keycloak-operations-mcp
    app.kubernetes.io/component: assessor
  annotations:
    description: >
      Service account used by keycloak-operations-mcp for read-only
      OpenShift / Kubernetes evidence collection (0.2.0+ collectors).
